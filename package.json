{
  "name": "mito-diet-app",
  "version": "1.0.0",
  "private": true,
  "description": "ミトコンドリア・ダイエット（個人用Webアプリ）のCapacitorモバイルラッパー",
  "scripts": {
    "sync:web": "cp index.html www/index.html",
    "cap:sync": "npm run sync:web && npx cap sync",
    "cap:open:ios": "npm run cap:sync && npx cap open ios",
    "cap:open:android": "npm run cap:sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2"
  }
}
